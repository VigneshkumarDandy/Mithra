#include<stdio.h>
#include<conio.h>
void main()
{
int number,a,b;
scanf("%d%d",&a,&b);
printf("Enter the Upper and Lower limits:");
for(number=a;number<=n;number++)
{
if(number%2==1)
printf("%d",number);
}
getch();
}
